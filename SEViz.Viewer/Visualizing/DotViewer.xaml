<UserControl x:Class="Rodemeyer.Visualizing.DotViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Rodemeyer.Visualizing">
  <Grid x:Name="Grid" >
    <ScrollViewer x:Name="ScrollViewer" Background="White"  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" >
      <local:GraphElement x:Name="DotGraph" />
    </ScrollViewer>
   
    <TextBlock x:Name="Statistics" 
               IsHitTestVisible="False" 
               Foreground="Gray" 
               HorizontalAlignment="Left" 
               VerticalAlignment="Bottom">
    </TextBlock>

    <TextBlock x:Name="Copyright" 
               IsHitTestVisible="False"
               Foreground="Gray"
               TextAlignment="Center" 
               HorizontalAlignment="Right" 
               VerticalAlignment="Bottom" >
      Powered by DotViewer © <LineBreak/>Christian Rodemeyer
    </TextBlock>

  </Grid>
</UserControl>
